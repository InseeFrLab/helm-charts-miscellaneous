[Interact with the service](https://{{ index .Values "llm-serving" "ingress" "hostname" }}/v1/)

{{- if index .Values "llm-serving" "ingress" "enabled" }}
If you intend to use open-webui you can set the environment var 

``` export OPENAI_API_BASE_URL=https://{{ index .Values "llm-serving" "ingress" "hostname" }}```
{{- end }}
